
Lists II
    Ordered collections of arbitrary objects
    Accessed by offset
    Variable-length, heterogeneous, and arbitrarily nestable
    Mutable.


    L * 3
    for x in L: print(x)
    3 in L
    Iteration, membership
    L.append(4)
    L.extend([5,6,7])
    L.insert(i, X)
    Methods: growing
    L.index(X)
    L.count(X)
    Methods: searching
    L.sort()
    L.reverse()
    L.copy()
    L.clear()
    Methods: sorting, reversing,
    copying (3.3+), clearing (3.3+)
    L.pop(i)
    L.remove(X)
    del L[i]
    del L[i:j]
    L[i:j] = []
    Methods, statements: shrinking
    L[i] = 3
    L[i:j] = [4,5,6]
    Index assignment, slice assignment
    L = [x**2 for x in range(5)]
    list(map(ord, 'spam'))

Excercises:

    4. Write a Python program that creates a list of your favorite foods and prints it out.
    Example output:
    ['pizza', 'sushi', 'tacos', 'ice cream']

    5. same as 4, include numbers also into list.

    6. create list of 10 items and slice from 5 to 6th position.

    7. 
